# Google Cloud Storage Configuration
# Path to your GCS service account key JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcs-service-account-key.json

# Default GCS bucket name (optional)
GCS_DEFAULT_BUCKET=your-bucket-name

# Google Drive API Configuration - Service Account (no OAuth needed!)
# Path to your Drive API service account key JSON file
GOOGLE_DRIVE_SERVICE_ACCOUNT=path/to/your/drive-service-account-key.json

# Upload Configuration
MAX_FILE_SIZE_MB=1
MAX_WORKERS=5

# Download Retry Configuration
# Number of retry attempts for failed downloads (default: 3)
MAX_DOWNLOAD_RETRIES=3
# Chunk size for downloads in bytes (default: 8192)
CHUNK_SIZE_BYTES=8192

# MongoDB Configuration
# MongoDB connection URL (default: mongodb://localhost:27017)
MONGODB_URL=mongodb://localhost:27017
# MongoDB database name (default: upload_to_cloud)
MONGODB_DATABASE=upload_to_cloud

# Server Configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# OpenAI Configuration
# Your OpenAI API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional OpenAI settings (defaults shown)
OPENAI_MODEL=gpt-4o-mini          # Model to use (gpt-4o, gpt-4o-mini, gpt-4, gpt-3.5-turbo)
OPENAI_MAX_TOKENS=2048            # Maximum tokens per request
OPENAI_TEMPERATURE=0.7            # Temperature for responses (0.0-1.0)
OPENAI_TIMEOUT_SECONDS=60         # Request timeout in seconds 