export enum ProjectStatus {
    notStarted,
    ongoing,
    finished,
    pending,
    error,
}

export enum StageStatus {
    notStarted,
    ongoing,
    finished,
    pending,
    error,
}

export enum StepStatus {
    notStarted,
    ongoing,
    finished,
    pending,
    error,
}
