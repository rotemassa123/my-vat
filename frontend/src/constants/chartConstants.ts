export const CHART_COLORS = {
  PRIMARY: '#6366f1',
  TEXT_SECONDARY: '#7f7f7f',
};

export const CHART_TYPOGRAPHY = {
  TICK_FONT_SIZE: '0.875rem',
  TICK_FONT_WEIGHT: 400,
};

export const CHART_MARGINS = {
  DEFAULT: { top: 10, bottom: 40, left: 50, right: 20 },
  PIE: { top: -20, bottom: 60, left: 0, right: 0 },
};

export const CHART_DIMENSIONS = {
  DEFAULT_WIDTH: 600,
  DEFAULT_HEIGHT: 400,
  PIE_WIDTH: 400,
  PIE_HEIGHT: 400,
};

export const Y_AXIS_SCALE = {
  BAR_MAX_RATIO: 0.7,
  LINE_MIN: 4000,
  LINE_MAX_RATIO: 0.7,
};

export const LINE_CHART_CONFIG = {
  MARKER_SIZE: 6,
};

